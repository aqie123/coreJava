# DAY1
    1. 自定义标识符
        a. 类名，接口名首字母大写
        b. 变量名, 方法名 首字母小写
        c. 包命名全部小写 
        d. 常量名 全部大写,下划线分隔
    2. 软件 = 数据 + 指令 + 说明文档
        a. 常量
            1. 整数常量
            2. 小数常量
            3. 布尔常量
            4. 字符常量 ： 单个字符使用单引号  'a'
            5. 字符串常量 ： 双引号
        2. 变量
    3. 字符存储原理
        1.码表 字符和数字映射
    4. 数据类型转换
        1. 小类型->大类型   自动类型转换
        2. 大->小  强制类型转换
        3. byte short char 数据类型运算,自动转换为int再运算
           所以 byte + byte 会报错
           byte b = 1; b += 1; 编译器 会进行强制类型转换
        4. 两个不同数据类型做运算,结果取大数据类型
            所以 int + long 会报错
        5. java编译器编译时可以检测到常量值,不能检测到变量值
            变量在虚拟机运行到该变量时才能在内存开辟空间,编译不会在内存开辟空间
            所以 int a = 1; byte b = a;会报错
    5. 负数及补码
        1. 负数最高位是1,正数最高位是0，负数计算机存的补码
        2. 补码运算
            a. 取负数绝对值,得到绝对值二进制(原码)
            b. 原码取反(反码)
            c. 反码 + 1 = 补码
    6.转义字符
        1. \b Backspace 退格键
        2. \t Tab  制表符
        3. \n 换行
        4. \r 回车  光标移动到首位
        5. WINDOWS下 \n\r 一起换行
    7.算数运算
        1. 取模结果正负取决于被除数
        2. 比较运算符
            1. &
            2. |
            3. || 短路或
            4. && 短路与
            5. ^  (异或:两边不一样)
            6. ~ 取反
            7. == 基本数据类型比较两个数内容是否相等
                  引用数据类型,比较的是两个数据的内存地址
        3. 位运算符
            1. &
            2. |
            3. ^ 异或操作两次还是操作数本身(用于加密解密)
            4. ~
        4,.移位运算符
            1. << 左移：相当于乘以 2的n次方
            2. >> 右移  左边空缺,如果是正数用0补,负数用1补
            3. >>> 无符号右移,都使用0补
        5. 三元运算符  布尔表达式 ？ 值 ： 值 ;
    8.  流程控制语句 
        1. switch ： 只能使用 byte char short int string
            
            永远先判断case语句,不管default语句顺序
            优点： 结构清晰,缺点：case 后面只能跟常量
        2. do while :
        3. break
            只能用于 switch 或者循环语句
        4. continue
            循环语句，跳过本次循环语句
        
    9. note :
        1. 字符串与字符串比较不能使用== 使用 equlas
    10.函数
    11. 数组 ()
        1. 只能存储同一类型数据的集合
        2. 一旦初始化，长度固定
        3. 数组中元素与元素的内存地址是连续的